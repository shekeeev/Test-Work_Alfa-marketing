import React, { FC } from 'react';
import s from './Header.module.scss'
const Header: FC = () => {
    return (
        <header>
            <h1>hh</h1>
        </header>
    );
};

export default Header;